/**
 * Run preamble-ts-core
 * The call to require below returns premalbe-ts-core's dist/src/main.js.
 * See the browserify task in preamble-ts-core's gulpfile.js, which generates the bundle.
 */
"use strict";
var boot = require("main");
boot();
